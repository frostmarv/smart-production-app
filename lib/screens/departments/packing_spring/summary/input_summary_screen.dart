// lib/screens/departments/packing_spring/summary/input_summary_screen.dart
import 'package:flutter/material.dart';

class InputSummaryScreen extends StatelessWidget {
  const InputSummaryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Input Summary - Packing Spring"),
      ),
      body: const Center(
        child: Text("Screen Input Summary belum diimplementasi"),
      ),
    );
  }
}
