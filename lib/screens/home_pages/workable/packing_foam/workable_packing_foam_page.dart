
import 'package:flutter/material.dart';

class WorkablePackingFoamPage extends StatelessWidget {
  const WorkablePackingFoamPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Workable Packing Foam'),
        backgroundColor: const Color(0xFFF1F5F9),
        foregroundColor: const Color(0xFF0F172A),
      ),
      body: const Center(
        child: Text('Workable Packing Foam Page - Coming Soon!'),
      ),
    );
  }
}
