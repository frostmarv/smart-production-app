
import 'package:flutter/material.dart';

class WorkablePackingSpringPage extends StatelessWidget {
  const WorkablePackingSpringPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Workable Packing Spring'),
        backgroundColor: const Color(0xFFF1F5F9),
        foregroundColor: const Color(0xFF0F172A),
      ),
      body: const Center(
        child: Text('Workable Packing Spring Page - Coming Soon!'),
      ),
    );
  }
}
